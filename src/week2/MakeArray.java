package week2;

class MakeArray {
	int[] makeArray() {
		int[] temp = new int[4];
		return temp;
	}
	//프로그램에서 메소드 사용하는 방법
	//1. 이름(입력) 		makeArray();
	//2. 출력 = 이름(입력) 	int[] lee; lee = makeArray();
}
